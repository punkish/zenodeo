'use strict';

const plugins = {
    _resource: 'author',
    _resources: 'authors',
    _resourceId: '',
    _name: 'authors2',
    _path: 'authors',
    _order: 12
};

const h = require('../lib/h3');
module.exports = h(plugins);