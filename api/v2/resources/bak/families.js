'use strict';

const plugins = {
    _resource: 'family',
    _resources: 'families',
    _resourceId: '',
    _name: 'families2',
    _path: 'families',
    _order: 8
};

const h = require('../lib/h3');
module.exports = h(plugins);